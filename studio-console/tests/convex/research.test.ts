import { test, expect } from "vitest";

test.skip("research run lifecycle (pending implementation)", async () => {
    // This will be replaced with a real integration test once `researchRuns`
    // lifecycle APIs (start/poll/cancel) are implemented and we add a proper
    // Convex test harness without extra dependencies.
    expect(true).toBe(true);
});
